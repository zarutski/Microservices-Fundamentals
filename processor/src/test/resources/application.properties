spring.kafka.bootstrap-servers=${KAFKA_DOCKER_SERVICE:localhost}:9093
resource-service-app.url=${RESOURCE_SERVICE_HOST:http://localhost:8091/}
song-service-app.url=${SONG_SERVICE_HOST:http://localhost:8093/}

kafka.consumer.group-id=songsapp
kafka.topic-upload=resource-upload
retry.backoff-period=2000
retry.max-attempts=3